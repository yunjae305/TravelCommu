<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원정보 수정</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/asset/style-register-fixprofile.css">
</head>
<body>
    <header class="top-header">
        <a href="/home" class="logo">
            <i class="fas fa-plane-departure"></i> 
            <span class="logo-text">Travel</span>
        </a>
    </header>

    <div class="bg-icon">
        <i class="fas fa-plane"></i>
    </div>

    <div class="register-design">
        <div class="register-box">
            <h1>회원정보수정</h1>
            <form action="/profile-fix" method="POST">
                <input type="text" name="name" placeholder="이름" value="<%= user.name %>" required>
                <select name="gender" required>
                    <option value="" disabled>성별 선택</option>
                    <option value="male" <%= user.gender === 'male' || user.gender === '남' ? 'selected' : '' %>>남성</option>
                    <option value="female" <%= user.gender === 'female' || user.gender === '여' ? 'selected' : '' %>>여성</option>
                </select>
                <input type="text" name="country" placeholder="선호국가" value="<%= user.country %>" required>
                <input type="email" name="email" placeholder="이메일" value="<%= user.email %>" readonly style="background-color: #e9ecef; color: #666; cursor: not-allowed;">
                <input type="text" name="id" placeholder="아이디" value="<%= user.userId %>" readonly style="background-color: #e9ecef; color: #666; cursor: not-allowed;">
                <input type="text" name="password" placeholder="비밀번호 (변경할 비밀번호 입력)" value="<%= user.password %>" required>
                <button type="submit" class="register-btn">수정하기</button>
            </form>

            <div class="back-link">
                <a href="/mypage">← 수정하지 않고 돌아가기</a>
            </div>
        </div>
    </div>
</body>
</html>