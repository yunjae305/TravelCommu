<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Travel Register' %></title>
    
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <link rel="stylesheet" href="/asset/style-register-fixprofile.css">
</head>
<body>
    <header class="top-header">
        <a href="/login" class="logo">
            <i class="fas fa-plane-departure"></i> 
            <span class="logo-text">Travel</span>
        </a>
    </header>
    <div class="bg-icon">
        <i class="fas fa-plane"></i>
    </div>
    <div class="register-design">
        <div class="register-box">
            <h1>회원가입</h1>
            <% if (error) { %>
              <div class="alert" id="error-msg"><%= error %></div>
            <% } %>
            <form id="signup-form" method="POST">
                <input type="text" id="name" name="name" placeholder="이름" value="<%= form?.name || '' %>">
                <select id="gender" name="gender">
                    <option value="" disabled <%= form?.gender ? '' : 'selected' %>>성별 선택</option>
                    <option value="남" <%= form?.gender === '남' ? 'selected' : '' %>>남성</option>
                    <option value="여" <%= form?.gender === '여' ? 'selected' : '' %>>여성</option>
                </select>
                <input type="country" id="country" name="country" placeholder="선호국가" value="<%= form?.country || ''%>">
                <input type="email" id="email" name="email" placeholder="이메일" value="<%= form?.email || '' %>">
                <input type="email" id="id" name="id" placeholder="아이디" value="<%= form?.id || '' %>">
                <input type="password" id="password" name="password" placeholder="비밀번호" value="<%= form?.id || '' %>">
                <button type="button" id="signup-btn" class="register-btn">가입하기</button>
            </form>
            <div class="back-link">
                <a href="/">← 로그인화면으로 돌아가기</a>
            </div>
        </div>
    </div>
    <script type="module" src="/firebase-client.js"></script>
    <script type="module" src="/signup.js"></script>
</body>
</html>
