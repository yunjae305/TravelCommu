<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'My Trips' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/asset/style-plan-list.css">
</head>
<body>
    <header class="top-header">
        <a href="/home" class="logo">
            <i class="fas fa-plane-departure"></i> 
            <span class="logo-text">Travel</span>
        </a>

        <div class="header-actions" style="display:flex; gap:12px; align-items:center;">
            <div id="header-user" style="display: flex; gap:8px; align-items:center;">
                <button class="header-right profile-btn" id="header-profile" onclick="location.href='/mypage'">
                    <div class="user-profile">
                        <i class="fas fa-user-circle"></i>
                        <span class="user-name" id="user-name" style="text-decoration:none;"><%= user.name %>님</span>
                    </div>
                </button>
            </div>
        </div>
    </header>
    
    <div class="bg-icon">
        <i class="fas fa-plane"></i>
    </div>
    
    <div class="main-container">
        <section class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-pen-nib" style="color: #005eff; margin-right: 8px;"></i> 
                    <span>내가 작성한 플랜</span>
                </h2>
                <a href="javascript:history.back();" class="see-more" style="color: #005eff; cursor: pointer;">← 돌아가기</a>
            </div>

            <table class="trip-table" id="my-trip-table">
              <thead>
                <tr>
                  <th style="width: 45%;">제목</th>
                  <th style="width: 15%;">참여인원</th>
                  <th style="width: 20%;">작성자</th>
                  <th style="width: 20%;">작성일</th>
                </tr>
              </thead>
              <tbody id="my-plans-body">
                <% if (trips && trips.length > 0) { %>
                    <% trips.forEach(planner => { %>
                        <tr onclick="location.href='/detail-myplan/<%= planner.id %>'" style="cursor: pointer;">
                            <td>
                                <span class="trip-title" style="font-weight:600; color:#0f172a;">
                                    <%= planner.topic %>
                                </span>
                            </td>
                            <td class="trip-meta">
                                <%= planner.participants ? planner.participants.length : 0 %> / <%= planner.headcount %>
                            </td>
                            <td class="trip-meta"><%= planner.authorName %></td>
                            <td class="trip-meta">
                                <%= planner.createdAt ? new Date(planner.createdAt).toLocaleDateString() : '-' %>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4" class="empty-row">'<%= user.name %>'님이 작성하신 여행 플랜이 없습니다.</td>
                    </tr>
                <% } %>
              </tbody>
            </table>
        </section>
    </div>

    <div class="floating-container">
        <a href="/write" class="floating-btn">
            <i class="fas fa-pen"></i>
            <span>플래너 작성</span>
        </a>
    </div>
  </body>
</html>
