<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Travel Main' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/asset/style-main.css">
</head>
<body>
    <header class="top-header">
        <a href="/home" class="logo">
            <i class="fas fa-plane-departure"></i> 
            <span class="logo-text">Travel</span>
        </a>

        <!-- 헤더 우측: 로그인 링크, 로그인 시 노출되는 프로필/로그아웃 -->
        <div class="header-actions" style="display:flex; gap:12px; align-items:center;">
            <div id="header-user" style="display: flex; gap:8px; align-items:center;">
                <button class="logout-btn" onclick="location.href='/logout'">
                    <div class="logout-profile">
                        <i class="fas fa-lock-open" style="margin-right: 5px;"></i>
                        <span>로그아웃</span>
                    </div>
                </button>
                <button class="header-right profile-btn" id="header-profile" onclick="location.href='/mypage'">
                    <div class="user-profile">
                        <i class="fas fa-user-circle"></i>
                        <span class="user-name" id="user-name" style="text-decoration:none;"><%= user.name %>님</span>
                    </div>
                </button>
            </div>
        </div>
    </header>
    
    <div class="bg-icon">
        <i class="fas fa-plane"></i>
    </div>
    
    <div class="main-container">
        <section class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-heart" style="color: #ff6b6b; margin-right: 8px;"></i>
                    <span>선호 여행국가 플랜 (<%= user ? user.country : '' %>)</span>
                </h2>
                <a href="/myfavorite" class="see-more">+ 더보기</a>
            </div>
            
            <div class="card-grid">
                <% if (user && favorites && favorites.length > 0) { %>
                    <% favorites.forEach(trip => { %>
                        <div class="card" onclick="location.href='/detail/<%= trip.id %>'">
                            <div class="card-image" style="background-image: url('https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?ixlib=rb-4.0.3');"></div>
                            <div class="card-content">
                                <div class="author-participants">
                                    <span class="headcount">
                                        <i class="fas fa-users"></i> 
                                        <%= trip.participants ? trip.participants.length+1 : 0 %> / <%= trip.headcount %>명
                                    </span>
                                    <span class="card-create-name">작성자: <%= trip.authorName %></span>
                                </div>
                                <h3 class="card-name"><%= trip.topic %></h3>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="card" style="cursor: default;">
                        <div class="card-image" style="background: linear-gradient(-60deg, #9795f0 0%, #fbc8d4 100%);">
                            <div class="emoji">😥</div>
                        </div>
                        <div class="card-content">
                            <h3 class="card-name">일치하는 플랜이 없습니다..</h3>
                        </div>
                    </div>
                <% } %>
            </div>
        </section>

        <section class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-pen-nib" style="color: #005eff; margin-right: 8px;"></i> 
                    <span>내가 작성한 플랜</span>
                </h2>
                <a href="/mytrip" class="see-more">+ 더보기</a>
            </div>
            
            <div class="card-grid">
                <% if (user && myPlans && myPlans.length > 0) { %>
                    <% myPlans.forEach(trip => { %>
                        <div class="card" onclick="location.href='/detail-myplan/<%= trip.id %>'">
                            <div class="card-image" style="background-image: url('https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?ixlib=rb-4.0.3');"></div>
                            <div class="card-content">
                                <span class="headcount">
                                    <i class="fas fa-users"></i> 
                                    <%= trip.participants ? trip.participants.length+1 : 0 %> / <%= trip.headcount %>명
                                </span>
                                <h3 class="card-name"><%= trip.topic %></h3>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="card" style="cursor: default;">
                        <div class="card-image" style="background: linear-gradient(60deg, #64b3f4 0%, #c2e59c 100%);">
                            <div class="emoji">😎</div>
                        </div>
                        <div class="card-content">
                            <h3 class="card-name">새로운 플랜을 작성해보세요!</h3>
                        </div>
                    </div>
                <% } %>
            </div>
        </section>

        <div class="floating-container">
            <a href="/write" class="floating-btn">
                <i class="fas fa-pen"></i>
                <span>플래너 작성</span>
            </a>
        </div>
    </div>
  </body>
</html>
