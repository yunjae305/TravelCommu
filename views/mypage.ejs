<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Travel Mypage' %></title>
    
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <link rel="stylesheet" href="/asset/style-mypage.css">
</head>
<body>
<header class="top-header">
        <div class="header-left">
            <a href="/home" class="logo">
                <i class="fas fa-plane-departure"></i> 
                <span class="logo-text">Travel</span>
            </a>
        </div>

        <div class="header-right">
            <button class="header-btn back-btn" onclick="location.href='/home'">
                <div class="back-profile">
                    <i class="fas fa-arrow-left"></i> 
                    <span>이전</span>
                </div>
            </button>
            <button class="logout-btn" onclick="location.href='/logout'">
                <div class="logout-profile">
                    <i class="fas fa-lock-open" style="margin-right: 5px;"></i>
                    <span>로그아웃</span>
                </div>
            </button>
        </div>
    </header>

    <div class="bg-icon">
        <i class="fas fa-plane"></i>
    </div>
    
    <div class="mypage-container">
        <section class="profile-section">
            <div class="profile-info">
                <span class="profile-img"></span>
                <div class="profile-text">
                    <h2>안녕하세요,<br><strong><%= user.name %>님</strong></h2>
                </div>
            </div>
            <a href="/profile-fix" class="change-profile">
                <i class="fas fa-pen"></i>
                <span class="change-profile-text">회원정보 수정</span>
            </a>
        </section>

        <% const fallbackImages = [
          'https://images.unsplash.com/photo-1507525428034-b723cf961d3e?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
          'https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
          'https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
          'https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80'
        ]; %>

        <section class="list-section">
            <div class="section-header">
                <h3 class="section-title">참여하고 있는 플래너</h3>
                <a href="/myfavorite" class="see-more">+ 더보기</a>
            </div>

            <div class="card-list">
                <% if (joinedPlans && joinedPlans.length > 0) { %>
                  <% joinedPlans.forEach((trip, index) => { %>
                    <% if (index < 3) { %>
                        <div class="list-card" onclick="location.href='/detail/<%= trip.id %>'">
                            <div class="card-img" style="background-image: url('<%= fallbackImages[index % fallbackImages.length] %>');"></div>
                            <div class="card-info">
                                <h4><%= trip.topic || trip.title %></h4>
                                <p><%= trip.destination || '여행지 미정' %></p>
                            </div>
                        </div>
                    <% } %>
                  <% }); %>
                <% } else { %>
                  <div class="list-card" style="cursor: default;">
                    <div class="card-img" style="background: linear-gradient(to left, #fbc2eb 0%, #a6c1ee 100%);"></div>
                    <div class="card-info">
                        <h4>참여중인 플래너가 없습니다 😱</h4>
                        <p>메인에서 탐색해보세요!</p>
                    </div>
                  </div>
                <% } %>
            </div>
        </section>

        <section class="list-section">
            <div class="section-header">
                <h3 class="section-title">작성한 플래너</h3>
                <a href="/mytrip" class="see-more">+ 더보기</a>
            </div>

            <div class="card-list">
                <% if (myPlans && myPlans.length > 0) { %>
                  <% myPlans.forEach((trip, index) => { %>
                    <% if (index < 3) { %>
                        <div class="list-card" onclick="location.href='/detail-myplan/<%= trip.id %>'">
                            <div class="card-img" style="background-image: url('<%= fallbackImages[(index + 1) % fallbackImages.length] %>');"></div>
                            <div class="card-info">
                                <h4><%= trip.topic || trip.title %></h4>
                                <p style="font-size: 0.9rem; color: #666; margin-top: 5px;">
                                    <i class="far fa-clock"></i> 
                                    <%= trip.createdAt ? new Date(trip.createdAt).toLocaleDateString() : '날짜 미정' %>
                                </p>
                            </div>
                        </div>
                    <% } %>
                  <% }); %>
                <% } else { %>
                  <div class="list-card" style="cursor: default;">
                      <div class="card-img" style="background: linear-gradient(120deg, #e0c3fc 0%, #8ec5fc 100%);"></div>
                      <div class="card-info">
                          <h4>작성한 플래너가 없습니다 😭</h4>
                          <p>'플래너 작성' 버튼을 눌러 직접 여행을 계획해보세요!</p>
                      </div>
                  </div>
                <% } %>
            </div>
        </section>
    </div>
</body>
</html>
